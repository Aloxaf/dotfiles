#compdef -p proxychains(_#)[0-9]#

_arguments \
    '-q[makes proxychains quiet - this overrides the config setting]' \
    '-f[allows one to manually specify a configfile to use]' \
    '(-)1:command: _command_names -e' \
    '*::arguments: _normal '
